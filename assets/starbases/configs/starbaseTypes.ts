import React from 'react';
import { StarbaseIcon } from '../icons';
import { StarbaseWireframe } from '../wireframes';
import { EntityTypeConfig } from '../../types';

export const starbaseType: EntityTypeConfig = {
    icon: StarbaseIcon,
    wireframe: StarbaseWireframe,
    colorClass: 'text-cyan-300'
};
